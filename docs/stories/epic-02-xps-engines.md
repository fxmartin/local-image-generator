# Epic 2: XPS Engine Spike & Local Backends

## Epic Overview
**Epic ID**: Epic-02
**Description**: Phase 0 and the local engines for Phase 1. First a hand-run spike on the Dell XPS 13 (Intel Arc 140V iGPU, 30 GB shared RAM) to measure stable-diffusion.cpp on Vulkan, `qwenimage-ncnn-vulkan`, and, as attempts, sd.cpp SYCL and PyTorch XPU. Then subprocess adapters for the engines that survive, tested against stub binaries so CI never needs a GPU.
**Business Value**: This epic decides whether the XPS can hit the ≤ 10 min per 1024² target at all. Doing the spike before any product code keeps the risk at the front, where it is cheap.
**Success Metrics**: `docs/bench/xps13.md` names the default XPS engine with measured load, per-step and total times; `lig generate --engine sdcpp|ncnn` produces a PNG on the XPS.
**Requirements**: P0-5, P0-9 (engine availability), NFR Performance targets (XPS), Risk table rows 1–2.

## Epic Scope
**Total Stories**: 8 | **Total Points**: 25 | **MVP Stories**: 8

## Features in This Epic

### Feature 02.1: Phase 0 Engine Spike

#### Stories

##### Story 02.1-001: Build stable-diffusion.cpp with Vulkan and hand-run one image on the XPS
**User Story**: As FX, I want stable-diffusion.cpp built with `-DSD_VULKAN=ON` on the XPS and one 1024² Qwen-Image-2.1 image generated by hand with the leejet Q4 GGUF set, so that I have a real timing on my weakest machine before writing product code.
**Priority**: Must Have
**Story Points**: 3

**Acceptance Criteria**:
- **Given** the XPS with `vulkan-intel`, `cmake`, `ninja` installed **When** sd.cpp is cloned at a pinned commit and built with Vulkan **Then** `sd-cli --help` runs and the build commit is recorded.
- **Given** the four files (Q4 transformer GGUF, `Qwen3VL-8B-Instruct-Q4_K_M.gguf`, `qwen_image_2.1_vae_bf16.safetensors`, mmproj F16) downloaded by hand **When** `sd-cli` runs the documented text-to-image command at 1024² with 40 steps, seed 42, cfg 6.0, Euler, `--offload-to-cpu` **Then** a PNG is produced and load time, per-step time, total time and peak RSS (from `/usr/bin/time -v`) are recorded in `docs/bench/xps13.md`.
- **Given** the same setup **When** the documented edit command runs on the produced PNG **Then** an edited PNG is produced and timed.
- **Given** an out-of-memory or driver failure **When** it happens **Then** the failure, the exact command and the fix attempted are recorded; a failure is a valid outcome of this story.

**Technical Notes**: Try `--model-args qwen_image_2_1_prefix_cache=false` as a second run to measure the prefix-cache memory/speed trade-off. Record `vulkaninfo --summary` device name and mesa version. Store SHA-256 of every downloaded file; Epic-03 reuses them.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: None
**Risk Level**: High

##### Story 02.1-002: Hand-run qwenimage-ncnn-vulkan on the XPS
**User Story**: As FX, I want the `qwenimage-ncnn-vulkan` release binary run on the XPS at 1024², 40 steps, seed 42, so that the full-BF16 Vulkan path is measured against sd.cpp on the same prompt.
**Priority**: Must Have
**Story Points**: 2

**Acceptance Criteria**:
- **Given** the pinned release binary and its model files **When** `qwenimage-ncnn-vulkan -p "<spike prompt>" -s 1024,1024 -l 40 -o out.png` runs **Then** a PNG is produced and timings plus peak RSS are recorded in `docs/bench/xps13.md`.
- **Given** the produced PNG **When** the edit form `-i out.png -p "<edit>"` runs **Then** the result and timing are recorded.
- **Given** the sd.cpp and ncnn outputs for the same prompt/seed **When** placed side by side in the bench doc **Then** a short quality note (sharpness, artifacts) accompanies the numbers.

**Technical Notes**: The project claims 2 GB dedicated VRAM is enough because weights stream from system memory; on an iGPU with shared memory this may be the best fit or the worst. Measure, do not assume.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: None
**Risk Level**: High

##### Story 02.1-003: Attempt sd.cpp SYCL and PyTorch XPU; adopt or reject with numbers
**User Story**: As FX, I want the two Intel-specific paths (sd.cpp built with `-DSD_SYCL=ON` on oneAPI, and diffusers on `torch` XPU) tried on the XPS, so that "best performance possible" is backed by having tested the vendor-optimised options, not only the portable ones.
**Priority**: Must Have
**Story Points**: 5

**Acceptance Criteria**:
- **Given** oneAPI Base Toolkit installed from the Arch repos or Intel's installer **When** sd.cpp is built with SYCL (`icx`/`icpx`, `-DGGML_SYCL_F16=ON`) **Then** either a timed 1024² run is recorded, or the blocking error and time spent are recorded and the path is marked rejected.
- **Given** a `uv` venv with a torch build that exposes `torch.xpu` **When** the diffusers `QwenImagePipeline` runs at 1024², 40 steps **Then** either a timed run is recorded, or the failure is recorded and the path is marked rejected.
- **Given** all attempts **When** the story closes **Then** the bench doc has one row per path with status `adopted`, `rejected: <reason>` or `deferred`, and the time-box (one working day total for both) was respected.
- **Given** a rejected path **When** documented **Then** the doc states what would change the decision (e.g. Intel validating OpenVINO, a torch release with Lunar Lake support).

**Technical Notes**: The Arc Pro B70 discrete card does 52 s per 1K image on XPU; the 140V iGPU shares 30 GB with the OS, so expect memory pressure with the bf16 text encoder. If torch XPU needs > 30 GB, reject without further tuning.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 02.1-001
**Risk Level**: Medium

##### Story 02.1-004: Publish the XPS bench report and pick the default engine
**User Story**: As FX, I want `docs/bench/xps13.md` finalised with a comparison table and a one-paragraph decision naming the default XPS engine, so that Epic-04 wires the right default and the 10-minute target is known to be realistic or not.
**Priority**: Must Have
**Story Points**: 2

**Acceptance Criteria**:
- **Given** the spike runs **When** the report is written **Then** it has a table with columns engine, build backend, weights set, size, steps, load s, s/step, total s, peak RSS GB, quality note, status.
- **Given** the fastest passing engine **When** total time ≤ 600 s at 1024² **Then** it is declared the XPS default and the built-in config default for Linux is set to it.
- **Given** no engine reaches ≤ 600 s at 1024² **When** the report is written **Then** it records the fallback chosen from the PRD (768² default, fewer steps, or XPS as remote-only client) and `REQUIREMENTS.md` §5 P0-1 is amended in the same commit.
- **Given** the report **When** read by someone else **Then** every number has the exact command and versions (sd.cpp commit, ncnn release, mesa, kernel) that produced it.

**Technical Notes**: This story is the gate between Phase 0 and Phase 1. Do not start Epic-04 stories that depend on real engines before it closes.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 02.1-001, 02.1-002, 02.1-003
**Risk Level**: Medium

### Feature 02.2: Subprocess Engine Adapters

#### Stories

##### Story 02.2-001: Generic subprocess engine runner
**User Story**: As FX, I want one runner that launches an engine binary with an argv list, streams its output to the log, parses progress lines into `on_progress` callbacks, enforces a timeout and kills the whole process group on cancel, so that every subprocess adapter shares the same robust plumbing.
**Priority**: Must Have
**Story Points**: 3

**Acceptance Criteria**:
- **Given** a stub binary that prints `step 3/40` style lines **When** run through the runner **Then** `on_progress(3, 40)` is called for each line and the full output lands in the log file.
- **Given** a timeout of N seconds **When** the binary exceeds it **Then** the runner sends SIGTERM to the process group, escalates to SIGKILL after a grace period, waits with headroom, and raises `EngineError("timeout")`.
- **Given** Ctrl-C **When** the runner is mid-run **Then** the child process group is terminated the same way and the partial output file, if any, is removed.
- **Given** a non-zero exit **When** the binary finishes **Then** `EngineError` carries the exit code and the last 20 lines of stderr.

**Technical Notes**: `subprocess.Popen(start_new_session=True)`; kill via `os.killpg`. Tests use a stub Python script as the binary; never assert that the child died promptly (CI has no init reaper).

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 01.2-002, 01.3-003
**Risk Level**: Medium

##### Story 02.2-002: stable-diffusion.cpp backend adapter
**User Story**: As FX, I want an `sdcpp` backend that maps `GenerateRequest`/`EditRequest` to `sd-cli` flags (diffusion model, VAE, LLM, mmproj for edits, size, steps, seed, cfg default 1.0, Euler, offload) and reports its version, so that the winning XPS engine is driven by the same CLI as every other engine.
**Priority**: Must Have
**Story Points**: 5

**Acceptance Criteria**:
- **Given** a `GenerateRequest` **When** `sdcpp.generate` runs against the stub binary **Then** the recorded argv contains `--diffusion-model`, `--vae`, `--llm`, `-p`, `-W`, `-H`, `--steps`, `-s`, `--cfg-scale 1.0`, `--sampling-method euler`, `-o <tmp.png>` with the values from the request and the registry paths; a request with `guidance` set passes that value instead.
- **Given** an `EditRequest` **When** `sdcpp.edit` runs **Then** argv additionally has `--llm_vision <mmproj>` and `-r <reference>`; without the mmproj file cached, `available()` reports `edit unsupported: mmproj missing`.
- **Given** the engine binary path **When** `available()` runs **Then** it checks the binary, the four weight files and the platform, and returns a reason string for the first failure.
- **Given** `sd-cli --version` or the build banner **When** parsed **Then** `ImageResult.engine_version` holds the commit/version string; unknown formats yield `unknown` rather than failing.
- **Given** config `engines.sdcpp.extra_args` **When** set **Then** the args are appended verbatim, so spike-discovered flags (`--model-args qwen_image_2_1_prefix_cache=false`) need no code change.

**Technical Notes**: Flag names come from `docs/qwen_image_2.1.md` in sd.cpp at the pinned commit; keep the mapping in one table so an upstream rename is a one-line fix. Transparent output is Epic-07. Default guidance is 1.0, not the 6.0 in sd.cpp's docs: Qwen-Image-2.1 is guidance-free, and on the XPS 1.0 halves the time (1248 s vs 2704 s at 1024², 40 steps) with no visible quality loss (see `docs/bench/xps13.md`).

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 02.2-001, 03.1-001, 02.1-004
**Risk Level**: Medium

##### Story 02.2-003: qwenimage-ncnn-vulkan backend adapter
**User Story**: As FX, I want an `ncnn` backend that maps requests to `qwenimage-ncnn-vulkan` flags (`-p`, `-s W,H`, `-l steps`, seed, `-i` for edits, `-o`) so that the full-precision Vulkan engine is a first-class, switchable option.
**Priority**: Must Have
**Story Points**: 3

**Acceptance Criteria**:
- **Given** a `GenerateRequest` **When** `ncnn.generate` runs against the stub **Then** argv matches the documented flags and the output PNG is read back into `ImageResult`.
- **Given** an `EditRequest` **When** `ncnn.edit` runs **Then** argv includes `-i <reference>`.
- **Given** the binary or its model directory is missing **When** `available()` runs **Then** the reason names the missing piece and the config key to set.
- **Given** the engine prints no per-step progress **When** running **Then** the CLI shows an indeterminate spinner with elapsed time rather than a fake percentage.

**Technical Notes**: Confirm the seed and guidance flags from the pinned release's `--help` during 02.1-002 and record them in the adapter's flag table.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 02.2-001, 03.1-001, 02.1-004
**Risk Level**: Medium

##### Story 02.2-004: Stub engine binaries for the test suite
**User Story**: As FX, I want stub `sd-cli` and `qwenimage-ncnn-vulkan` scripts under `tests/stubs/` that echo their argv to a file, print fake progress lines and write a small valid PNG to the `-o` path, so that adapters are tested end-to-end in CI without GPUs or weights.
**Priority**: Must Have
**Story Points**: 2

**Acceptance Criteria**:
- **Given** the stub on `PATH` via a fixture **When** an adapter runs **Then** the test can assert the exact argv from the echo file.
- **Given** `STUB_FAIL=1` in the environment **When** the stub runs **Then** it prints an error to stderr and exits 2, exercising `EngineError`.
- **Given** `STUB_SLEEP=<s>` **When** set **Then** the stub sleeps, enabling timeout and cancel tests with generous headroom.
- **Given** the stubs **When** executed in the CI container **Then** they need only `python3` and Pillow (already test deps) and no `bash`.

**Technical Notes**: Stubs are Python files with a `#!/usr/bin/env python3` shebang; the fixture copies them to a temp dir with the real binary names and `chmod +x`.

**Definition of Done**:
- [ ] Code implemented and peer reviewed
- [ ] Tests written and passing
- [ ] User-facing docs updated in the same commit for behavior-changing diffs (README/docs/usage/help; CHANGELOG excluded — Epic-05 owns it)

**Dependencies**: 01.1-001
**Risk Level**: Low

## Sprint Breakdown
| Sprint | Stories | Points | Status |
|---|---|---|---|
| 1 (Phase 0) | 02.1-001, 02.1-002, 02.1-003, 02.1-004 | 12 | Not started |
| 3 | 02.2-004, 02.2-001, 02.2-002, 02.2-003 | 13 | Not started |

## Epic Progress
- [x] 02.1-001 (3) · [x] 02.1-002 (2) · [x] 02.1-003 (5) · [x] 02.1-004 (2) · [ ] 02.2-001 (3) · [ ] 02.2-002 (5) · [ ] 02.2-003 (3) · [ ] 02.2-004 (2)
- **Completed**: 12 / 25 points (02.1-001 done: sd.cpp works, 21 min at 1024² with guidance 1.0, edit failed on memory; 02.1-002 rejected on this hardware; 02.1-003 SYCL and torch XPU both rejected; 02.1-004 decided: remote by default, local 768²/30 steps fallback at 398 s; see `docs/bench/xps13.md`)
